[build]
language = "python"

[phases.setup]
nixPkgs = ["python311Full", "python311Packages.pip", "gcc", "redis"]

[phases.install]
cmds = [
  "pip install --no-cache-dir -r requirements.txt",
  "pip install --no-cache-dir redis hiredis"  # Fast Redis client
]

[phases.build]
cmds = []

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port 8000 --workers 2"
